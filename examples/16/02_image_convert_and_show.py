# ============================================
# K230 Example
# Автор: AIDevelopersMonster
# Плата: Yahboom K230
# GitHub https://github.com/AIDevelopersMonster/K230            
#
# Описание:
# Демонстрация преобразования формата изображения.
# Скрипт показывает, как конвертировать изображение из одного
# цветового формата в другой (RGB888) для корректного отображения.
#
# Используется:
# - Display (дисплей ST7701)
# - MediaManager (управление медиа)
# - image (загрузка и обработка изображений)
#
# ============================================

# Импорт необходимых библиотек для работы с дисплеем и медиа
from media.display import *
from media.media import *
import image
import time

# ============================================
# Инициализация оборудования
# ============================================

# Инициализация дисплея с параметрами:
# - Display.ST7701: тип контроллера дисплея
# - width=640, height=480: разрешение экрана в пикселях
# - osd_num=1: количество слоёв OSD (On-Screen Display)
# - to_ide=True: режим работы с IDE для отладки
Display.init(Display.ST7701, width=640, height=480, osd_num=1, to_ide=True)

# Инициализация менеджера медиа - необходим для работы с файлами и изображениями
MediaManager.init()

# ============================================
# Загрузка и преобразование изображения
# ============================================

# Загружаем изображение из файла на SD-карте
# Параметры:
# - '/sdcard/resources/wp.png': путь к файлу изображения
# Примечание: в этом примере не используем copy_to_fb,
# так как сначала нужно преобразовать формат
img = image.Image('/sdcard/resources/wp.png')

# Преобразуем изображение в формат RGB888
# RGB888 - это 24-битный цвет:
# - 8 бит на красный канал (Red)
# - 8 бит на зелёный канал (Green)
# - 8 бит на синий канал (Blue)
# Этот формат требуется для корректного отображения на дисплее K230
img_rgb = img.to_rgb888()

# Выводим сообщение в консоль о successful конвертации
print('Converted to RGB888')

# ============================================
# Отображение изображения
# ============================================

# Показываем преобразованное изображение на экране
# show_image() принимает объект изображения и выводит его на дисплей
Display.show_image(img_rgb)

# ============================================
# Основной цикл программы
# ============================================

# Бесконечный цикл для поддержания работы программы
# Без этого цикла программа завершится и изображение пропадёт
# time.sleep(1) приостанавливает выполнение на 1 секунду в каждой итерации,
# чтобы не нагружать процессор
while True:
    time.sleep(1)
