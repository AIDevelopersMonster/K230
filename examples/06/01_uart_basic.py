# ============================================
# K230 Example
# Автор: AIDevelopersMonster
# Плата: Yahboom K230
# GitHub https://github.com/AIDevelopersMonster/K230 
#
# Описание:
# Базовая работа с UART - отправляет приветственное сообщение
# и выводит все данные, полученные через UART интерфейс
#
# Используется:
# - YbUart
#
# ============================================

from ybUtils.YbUart import YbUart  # Импорт класса для работы с UART
import time  # Импорт модуля времени для задержек

# Создаем объект UART с скоростью передачи 115200 бит/сек
uart = YbUart(baudrate=115200)

# Отправляем приветственное сообщение через UART
uart.send("Hello K230 UART\n")

# Бесконечный цикл для постоянного чтения данных
while True:
    # Читаем данные из UART (если что-то пришло)
    data = uart.read()
    
    # Если данные получены (не пустые)
    if data:
        # Декодируем байты в строку и выводим на экран
        print(data.decode())
    
    # Небольшая задержка 50 мс, чтобы не перегружать процессор
    time.sleep_ms(50)
